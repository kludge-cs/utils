/* TODO: add proper unit testing.
   as of present, due to graphql integration, there is no way to do so without
   making unnecessary or false requests to the github api, which is undesirable. */

describe("nothing", () => {
	test("nothing", () => {
		return undefined;
	});
});
